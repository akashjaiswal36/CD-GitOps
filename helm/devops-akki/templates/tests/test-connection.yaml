apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-akki.fullname" . }}-test-connection"
  labels:
    {{- include "devops-akki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-akki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
